// build image - where image name will be pig_game

docker build -t=pig_game:v1.0.0.0 .

// run image - exposing over port 3000
// -i stands for interactive mode and -t will allocate a pseudo terminal

docker run -d --name pig_game -p 80:80 pig_game:v1.0.0.0
